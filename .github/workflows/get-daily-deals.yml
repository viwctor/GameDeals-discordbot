name: Post Game Deals (every 5 days)

on:
  schedule:
    # Run at 14:00 UTC every 5th day of the month
    - cron: "17 */3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  post-deals:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Enable Yarn
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build TypeScript
        run: yarn build

      - name: Post deals to Discord
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
          ITAD_API_KEY: ${{ secrets.ITAD_API_KEY }}
          DEAL_LIMIT: ${{ vars.DEAL_LIMIT || '50' }}
          MIN_SAVINGS: ${{ vars.MIN_SAVINGS || '30' }}
          MAX_SAVINGS: ${{ vars.MAX_SAVINGS || '85' }}
          COUNTRY: ${{ vars.COUNTRY || 'US' }}
          SHOP_IDS: ${{ vars.SHOP_IDS || '61,35,37,24,29,36,49' }}
          DEDUPLICATION_DAYS: ${{ vars.DEDUPLICATION_DAYS || '5' }}
        run: yarn start

      - name: Commit deal history
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add deal-history.json

          if ! git diff --cached --quiet; then
            git commit -m "Update deal history [skip ci]"
            git push
          else
            echo "No deal history changes to commit."
          fi
